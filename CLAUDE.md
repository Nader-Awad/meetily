# CLAUDE.md

This project intentionally keeps CLAUDE.md **minimal**. Durable project knowledge —
architecture, conventions, gotchas, build commands, module maps — lives in **NeoHive
cognitive memory** and is retrieved on demand, so each session loads only what's
relevant to the task at hand instead of a static wall of documentation.

**Your first action every session:** call `memory_context` with a description of your
task, then `memory_recall` for specifics. The topology block below tells you which hive
answers which kind of query and where new knowledge should be written.

<!-- BEGIN neohive-managed v=1 -->
<!-- Generated by /neohive:generate-claude-md on 2026-07-03. Re-run to refresh. -->

## NeoHive Cognitive Memory — Project Topology

Generic NeoHive tool-usage rules are loaded from `~/.claude/rules/hivemind.md`.
This block adds the **project-specific** topology and routing that determine
WHICH hive serves which query, and where new writes should land.

### Hive Topology

| Hive (UUID) | Name | Type | Embedding model | What it holds | Write to it? |
|---|---|---|---|---|---|
| `280166b7-7928-4b3c-a071-30dc6985f8e2` | Knowledge | knowledge | `nomic-ai/nomic-embed-text-v1.5` (prose-tuned) | Curated, LLM/agent-authored insights, conventions, decisions, and learnings. Seeded 2026-07-03 with 15 memories migrated from the original CLAUDE.md: audio pipeline & mixing, Tauri IPC (command/event) patterns, Whisper model management, naming/logging/error/git conventions, dev/build commands, and the audio module map. This is where durable knowledge accumulates over time. | **YES** — default write target. `memory_store` lands here automatically; do not pass an explicit `hive`. |
| `253400fc-e6ab-45aa-b76c-20eee071b04b` | Meetily Fork | repo | `google/embeddinggemma-300m-code` (code-tuned) | Full Meetily codebase index (~6400 memories): mostly `narrative` source-file chunks, plus auto-extracted `directive`/`convention`/`decision`/`insight` chunks from files like `pipeline.rs`, `recording_commands.rs`, `WhisperModelManager.tsx`, `ffmpeg_mixer.rs`. Ground truth for "where is X / how does the current code work". | **NO** — auto-managed by indexing; manual writes risk being overwritten on re-index. Read-only. |

**Why query phrasing matters here.** Because the two hives use different embedding
models, phrase queries to match the hive you expect to answer. For **Meetily Fork**
(code-tuned) lead with real code tokens and symbols — e.g. `AudioMixerRingBuffer VecDeque window`,
`start_recording tauri::command generate_handler`, `WhisperEngine load_model GPU fallback`. For
**Knowledge** (prose-tuned) use affirmative natural-language statements — e.g.
`audio device naming convention microphone system`, `Whisper models cached restart to swap`. When
unsure which hive holds the answer, pass BOTH phrasings via the `queries` parameter and let
cross-hive RRF fusion rank them.

**Interpreting `[hive: <uuid>]` in recall results.** A hit tagged `253400fc…` (**Meetily Fork**)
is indexed source code or docs — treat it as factual about the *current* codebase, but remember
`narrative` hits are raw file excerpts, not curated advice, and can include stale comments. A hit
tagged `280166b7…` (**Knowledge**) is curated, human/agent-authored knowledge — conventions,
decisions, and gotchas meant to be trusted as guidance.

### Session Start — Non-Negotiable (Project-Specific)

1. `memory_context` is your FIRST action — see `~/.claude/rules/hivemind.md`.
2. Confirm the topology above has not drifted: call `list_hives` once per session.
   If a hive is added / removed / renamed, re-run `/neohive:generate-claude-md`.
3. Follow up with a targeted `memory_recall` for this project's domain. Suggested seeds:
   - `Meetily development build run commands clean_run.sh pnpm tauri:dev`
   - `audio pipeline VAD filtering vs recording mixing path`
   - `Tauri command registration invoke_handler generate_handler`
   - `Whisper model loading GPU Metal CUDA fallback caching`
   - `ScreenCaptureKit macOS system audio BlackHole permissions`

### What Goes Where: Cognitive Memory vs CLAUDE.md

| Store in **Cognitive Memory** (Knowledge hive) | Store in **CLAUDE.md** |
|---|---|
| Durable conventions, gotchas, and decisions discovered while working (e.g. "audio devices are named microphone/system", "Whisper models are cached — restart to swap"). | Only the minimal orientation above: the pointer to NeoHive and this topology block. |
| Session learnings, corrections, and non-obvious insights that future sessions should recall automatically. | Nothing that changes often — that belongs in the hive so it's retrieved fresh, not stale. |
| Anything you'd want surfaced automatically by `memory_context` at session start. | The canonical routing map (this file), kept intentionally small. |

### Hive routing for writes

Writes default to **`Knowledge`** — it is the only write-safe hive, and `memory_store`
targets it automatically; the Meetily Fork hive is a read-only code index that would
overwrite manual writes on re-index.
**Do not pass an explicit `hive` parameter to `memory_store` unless you have a
specific reason.** When you do, write one sentence in the memory body explaining why.

<!-- END neohive-managed v=1 -->
